
@using Microsoft.AspNet.Identity
@using Predict.Models
@using System.Linq;
@model IEnumerable<Predict.Models.Event>

@{
    ViewBag.Title = "Add/Edit Comps";
}
<nav aria-label="breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item">
            <a href="/EventPlayers">Your Comps</a>
        </li>
        <li class="breadcrumb-item active" aria-current="page">Comps List</li>
    </ol>
</nav>

<input class="form-control" id="myInput" type="text" placeholder="Search..">

@{
    var playerId = User.Identity.GetUserId();
}
@using (Html.BeginForm("SaveEvent", "Events"))

{
    @Html.AntiForgeryToken()

    <table class="table" id="events">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.EventName)
                </th>
                <th class="hidden-xs">Start Date</th>
                <th class="hidden-xs">End Date</th>
            </tr>
        </thead>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.EventName)
                </td>
                <td class="hidden-xs">
                    @item.StartDateTime.ToString("f")
                </td>
                <td class="hidden-xs">
                    @item.EndDateTime.ToString("f")
                </td>
                <td>

                    <div class="btn-group">
                        <button type="button" class="btn btn-default">Action</button>
                        <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown">
                            <span class="caret"></span>
                            <span class="sr-only">Toggle Dropdown</span>
                        </button>
                        <ul class="dropdown-menu" role="menu">
                            <li><a href="@Url.Action("Index", "EventFixtures", new {id = item.Id}) ">Add Fixtures</a></li>
                            <li><a href="@Url.Action("GroupGameResults", "Results", new {id = item.Id}) ">Show Fixtures</a></li>
                            @if (@User.IsInRole("Admin"))
                            {
                                <li><a href="@Url.Action("Index", "KoFixtures", new {id = item.Id}) ">KO Fixtures</a></li>
                            }
                            <li><a href="@Url.Action("EditEvent", "Events", new {id = item.Id})">Edit</a></li>
                            <li class="divider"></li>
                            <li class="js-delete" dataEventId="@item.Id" dataEventName="@item.EventName"><a href="#">Delete Comp</a></li>
                        </ul>
                    </div>

                </td>
            </tr>
        }
    </table>
}

<input type="button"
       id="add"
       value="Create New Comp"
       class="btn btn-primary alignright"
       onclick="location.href = '@Url.Action("AddEvent", "Events") '" />

<script src="~/Scripts/bootbox.js"></script>


<script>

    $(document).ready(function () {
        $("#myInput").on("keyup", function () {
            var value = $(this).val().toLowerCase();
            $("#events tr").filter(function () {
                $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1)
            });
        });
    });

</script>

<script>


    $("#Events").on("click",
        ".js-delete",
        function () {
            var button = $(this);
            bootbox.confirm("Are you sure you want to delete the " + button.attr("dataEventName") + " comp?",
                function (result) {
                    if (result) {
                        $.ajax(
                            {
                                url: "../api/events/delete/" + button.attr("dataEventID"),
                                method: "POST",
                                success: function () {
                                    $(button.closest("tr")).fadeOut(1000);
                                }
                            });
                    }

                });
        });

</script>