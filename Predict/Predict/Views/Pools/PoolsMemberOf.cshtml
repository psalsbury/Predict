@using Microsoft.Ajax.Utilities
@using Microsoft.AspNet.Identity
@model IEnumerable<Predict.Models.PoolPlayer>

@{
    ViewBag.Title = "League Membership";
    var rowNbr = 1;
}

<nav aria-label="breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item">
            <a href="~/PoolDashboard">League Admin</a>
        </li>

        <li class="breadcrumb-item active" aria-current="page">Your League</li>
    </ol>
</nav>

<input class="form-control" id="myInput" type="text" placeholder="Search..">

<div class="col-md-4">
    <table class="table table-bordered" id="poolMembershipJoined">
        <thead class="thead-dark">
            <tr>
                <th>
                    Nbr
                </th>
                <th>
                    League Name
                </th>
            </tr>
        </thead>

        @foreach (var joinedPool in @Model)
        {

            <tr>
                <td>
                    <h4>
                        @rowNbr
                    </h4>

                </td>
                <td>
                    <h4>
                        @Html.DisplayFor(modelItem => joinedPool.Pool.PoolName)
                    </h4>
                </td>
                <td>
                    @if (!joinedPool.Pool.DefaultPoolForEvent)
                    {
                    <button class="btn btn-danger js-delete btn-sm" value="Join" dataPlayerId="@joinedPool.PlayerId" dataPoolId="@joinedPool.Pool.Id" poolName="@joinedPool.Pool.PoolName">

                        <b>Leave League</b>&nbsp;
                        <i class="fa fa-trash"></i>
                    </button>
                    }


                </td>

            </tr>
            rowNbr += 1;
        }
    </table>
    </div>

    <script src="~/Scripts/bootbox.js"></script>

    <script>

        $(document).ready(function () {
            $("#myInput").on("keyup", function () {
                var value = $(this).val().toLowerCase();
                $("#poolMembershipJoined tr").filter(function () {
                    $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1)
                });
            });
        });

    </script>

    <script>


    $("#poolMembershipJoined").on("click",
        ".js-delete",
        function() {
            var button = $(this);
            bootbox.confirm("Are you sure you want to leave the " + button.attr("poolName") + " league?",
                function(result) {
                    if (result) {
                        $.ajax(
                            {
                                url: "../api/poolPlayers/delete/" + button.attr("dataPoolId") + "/@(User.Identity.GetUserId())",
                                method: "POST",
                                success: function() {
                                $(button.closest("tr")).fadeOut(1000);
                            }
                        });
                    }

                });
        });



    </script>
