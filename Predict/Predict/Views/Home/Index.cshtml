@model Predict.Models.EventPlayer
<div class="style:">
    @if (Context.User.Identity.IsAuthenticated)
    {
        @Html.Partial("LoggedOn")
    }
    else
    {
        @Html.Partial("NotLoggedOn")
    }
</div>