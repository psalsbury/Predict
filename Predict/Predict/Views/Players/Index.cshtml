@model List<Predict.Models.Player>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<table class="table table-bordered table-hover">
    <tbhead>
        <tr>
            <td>Player Name</td>
        </tr>
    </tbhead>
    @foreach (var player in Model)
    {
        <tr>
            @*
        <td>@Html.ActionLink(@player.PlayerName, "EditPlayer", "Players", new { id = player.Id }, null) </td>
        *@
            <td>@player.AspNetUser.Email</td>
        </tr>
    }

</table>